// 这是一个虚拟的人机。
#include <MyFiveChess.h>
namespace AI{
    // 内部的演算棋盘，第一步为棋盘现在的状态

    // 调节AI的“聪明”程度

    // 返回下一步应该走的位置
    point move(board& b);

    // 扫描整个棋盘，找到连3和连4的对面棋子和我方棋子
    
    // 根据以上信息对棋盘棋子重新得分赋予值
};